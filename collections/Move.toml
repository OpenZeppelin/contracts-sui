[package]
name = "openzeppelin_collections"
edition = "2024"

# The Sui framework + MoveStdlib are implicit system dependencies in Move 2024.
# `sorted_set` consumes `sorted_map` as a sibling module within this same package,
# so there is no cross-package dependency to declare.
