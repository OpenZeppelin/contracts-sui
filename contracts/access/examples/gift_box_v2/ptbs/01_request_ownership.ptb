# Requires environment variables (all with 0x prefix):
#   PACKAGE_ID        -> package ID containing the modules
#   OWNER_CAP_ID      -> ID of the OwnerCap<GIFT_BOX_V2>
#   OWNER_ADDRESS     -> current owner's address
--assign cap_id @${OWNER_CAP_ID}
--assign current_owner @${OWNER_ADDRESS}
--move-call ${PACKAGE_ID}::ownable::request_ownership cap_id current_owner
--gas-budget 500000000
